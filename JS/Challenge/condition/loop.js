const links = {
  about: "About",
  policy: "Privacy Policy",
  contact: "Contact Us",
};

Object.entries(links).forEach((key, value) => {
  console.log(key);
  console.log(value);
});

// links.map((item) => {
//   console.log(item);
// });
